[general]
name = "math"
version = "2.0.0"
fix_version = "1.5.0"
description = "Provides wrapper of C functions defined in math.h."

[build]
files = ["math.fix"]
dynamic_links = ["m"]

[build.test]
files = ["test.fix"]
