[general]
name = "math"
version = "1.2.1"
fix_version = "1.5.0"
description = "Provides wrapper of C functions defined in math.h."

[build]
files = ["math.fix"]
dynamic_links = ["m"]

[build.test]
files = ["test.fix"]
